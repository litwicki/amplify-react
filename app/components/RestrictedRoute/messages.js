/*
 * RestrictedRoute Messages
 *
 * This contains all the text for the RestrictedRoute component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RestrictedRoute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RestrictedRoute component!',
  },
});
