/*
 * SignOut Messages
 *
 * This contains all the text for the SignOut component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SignOut';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SignOut component!',
  },
});
